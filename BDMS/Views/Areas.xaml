<Window x:Class="BDMS.Views.Areas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Areas" Height="Auto" Width="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0">Area Code: </Label>
            <TextBox Grid.Column="1" Margin="0,0,0,25" />
            <Label Grid.Column="1" VerticalAlignment="Bottom">Error</Label>
            <Label Grid.Row="1">Area Name: </Label>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,25" />
            <Label Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom">Error</Label>
            
        </Grid>
        <DataGrid Grid.Column="2"></DataGrid>
    </Grid>
</Window>
